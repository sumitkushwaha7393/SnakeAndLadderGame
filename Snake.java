package src;

public class Snake {
    int currentPosition;
    int destinationPosition;


    public Snake(int currentPosition, int destinationPosition) {
        this.currentPosition = currentPosition;
        this.destinationPosition = destinationPosition;
    }
}
